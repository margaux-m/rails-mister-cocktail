<%= simple_form_for [cocktail, dose] do |f| %>
  <%= f.association :ingredient, required: true, input_html: { class: 'select2' } %>
  <%= f.input :description, required: true %>
  <%= f.button :submit, 'Add ingredient', class: "btn btn-primary" %>
<% end %>
